# Abyz-AutoRA Environment Configuration
# Copy this file to .env and modify values
# IMPORTANT: .env file is excluded from git for security

# ===== REQUIRED SECURITY SETTINGS =====
# Change these values before deployment!

# n8n Authentication
N8N_USER=admin
N8N_PASSWORD=CHANGE_THIS_PASSWORD_IMMEDIATELY

# Encryption key for n8n data (generate with: openssl rand -hex 32)
ENCRYPTION_KEY=CHANGE_THIS_TO_RANDOM_32_CHAR_HEX_KEY

# ===== NAS Mount Configuration =====
# Update this path to match your NAS mount point
NAS_PATH=/mnt/medical-auth

# ===== Network Configuration =====
# Update hostname to match your Raspberry Pi
N8N_HOST=raspberrypi.local
N8N_PORT=5678
WEBHOOK_URL=http://raspberrypi.local:5678/
EDITOR_BASE_URL=/

# ===== Locale Settings =====
TIMEZONE=Asia/Seoul
LOG_LEVEL=warn

# ===== Optional: PostgreSQL Database =====
# Uncomment to use PostgreSQL instead of SQLite
# DB_TYPE=postgresdb
# DB_POSTGRESDB_HOST=postgres
# DB_POSTGRESDB_PORT=5432
# DB_POSTGRESDB_DATABASE=n8n
# DB_POSTGRESDB_USER=n8n
# DB_POSTGRESDB_PASSWORD=your_secure_postgres_password

# ===== Watchtower Configuration =====
# Watchtower is disabled by default for security
# To enable, add profile: --profile auto-update to docker-compose up
# WATCHTOWER_SCOPE=abyz-autora
